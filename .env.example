# Optional: for Word Library, unknown words are defined via Anthropic API.
# Get your key at https://console.anthropic.com/
# Without this, only words in the built-in vocabulary can be added to the Word Library.
VITE_ANTHROPIC_API_KEY=
